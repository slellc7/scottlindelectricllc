{
  "name": "scottlind-electric",
  "version": "1.0.0",
  "private": true,
  "description": "Scott Lind Electric LLC — static marketing site, built from the Claude Design source.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "firebase:check": "npm --prefix functions run check",
    "firebase:emulators": "npm --prefix functions run serve",
    "firebase:deploy": "npm --prefix functions run deploy",
    "astro": "astro"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
